// Token refresh and auth interceptors are handled directly in src/lib/client.ts
// on the apiClient Axios instance. This file is kept as a stub so existing
// imports do not break, but it performs no work.

export function setupTokenInterceptor() {}
export function setupAuthInterceptor() {}
export function initializeAuthInterceptors() {}
